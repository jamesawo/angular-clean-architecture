import { ComponentFixture, TestBed } from '@angular/core/testing';

describe('PostComponent', () => {

    describe('ngOnInit', () => {
        it('should get route param from Router', () => { });
        it('should call Router.navigateByUrl if route param is NOT present');
        it('should call interactor.getOne with route param as input if present')
    });

    describe('setSeo', () => {
        it('should call seoService.setSEO method', () => { });
        it('should an object to setSEO method of seoService', () => { });
    })
});
